name: Build and publish to Chrome Webstore

on:
  push:
    branches: ["main", "prerelease"]
  pull_request:
    branches: ["main", "prerelease"]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      - run: npm ci
      - run: npm run build --if-present

      # Package the build output into a zip file with a date-based name.
      - name: Package extension
        id: package
        run: |
          BASE_NAME="$(date +'%Y-%m-%d')_tj-volume-mixer"
          FINAL_ZIP_FILE="${BASE_NAME}.zip"
          cd build && zip -r "../${FINAL_ZIP_FILE}" . && cd ..
          echo "zip_file=${FINAL_ZIP_FILE}" >> $GITHUB_OUTPUT

      # Upload the zip file as an artifact (retained for 1 day)
      - name: Upload build zip file
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.package.outputs.zip_file }}
          path: ${{ steps.package.outputs.zip_file }}
          retention-days: 1

      # Publish the extension to the Chrome Web Store
      - name: Upload to Chrome Web Store
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: mobilefirstllc/cws-publish@latest
        with:
          action: "upload" # or "publish", "testers" as needed
          client_id: ${{ secrets.CLIENT_ID }}
          client_secret: ${{ secrets.CLIENT_SECRET }}
          refresh_token: ${{ secrets.REFRESH_TOKEN }}
          extension_id: ${{ secrets.EXTENSION_ID }}
          zip_file: ${{ steps.package.outputs.zip_file }}
